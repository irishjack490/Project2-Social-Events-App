<%- include('../partials/header') %>

<div class="container center">
    <h2><% events._embedded.events[0].image %></h2>
    <hr>
    <table class="table table-striped mt-3">
        <thead>
            <tr>
               
                <th scope="col">Event name</th> 
                <th scope="col">Date</th>  
                <th scope="col">Time</th> 
                <th scope="col">Classification</th>
                <th scope="col">Venue</th>
                              
                <!-- <th scope="col" class="remEv">Remove Event</th> -->
                
                
            </tr>
        </thead>
        <tbody>
            <!-- <% events.forEach((e) => { %>
                <% const Interested = true; %>
                <tr>
                    <!--other event objects were not readable, it appears the info was saved into a type array in Mongo-->
                    <td scope="row"><%= e.type %></td>
                    <td scope="row"><%= e.type %></td>
                    <td scope="row"><%= Interested ? 'Y' : 'N' %></td>
                    <td scope="row"></td>
                    <td scope="row">
                    <div-btn class="group">
                        <form  action="/events/delete/<%= e.type %>?_method=DELETE" method="POST">
                        <input type="submit" class="btn btn-danger" value="delete this event"></form>
                    </div-btn>
                    </td>       
                
                
                </tr>
            <% }) %>
           -->
        </tbody>
    </table>
</div>

<%- include('../partials/footer') %>

