<%- include('../partials/header') %>
<div class="center-cards">
  <div class="row row-cols-1 row-cols-md-2 g-4">
    <% if(events && events._embedded && Array.isArray(events._embedded.events) && events._embedded.events.length > 0) { %>
      <% events._embedded.events.forEach(function(event) { %>
        <div class="col">
          <div class="card custom-card">
            <img src="<%= event.images[0].url %>" class="card-img-top" alt="event-image">
            <div class="card-body">
              <h5 class="card-title"><%= event.name %></h5>
              <h6 class="card-title"><%= event.dates.start.localDate %></h6>
              <h6 class ="card-text"><%= event.dates.start.localTime %></h6>
              <h6 class ="card-text"><%= event._embedded.venues[0].name %></h6>
              <h6 class ="card-text"><%= event._embedded.venues[0].address.line1 %></h6>
              <p class="card-text"></p>
          <!--Takes use to another page for more details-->
          <a href="/events/<%= event.name %>" class="btn btn-primary">Interested</a>
           <form>
              <fieldset>
    
                <input hidden type="text" name="name" value="<%= event.name %>">
                <input hidden type="text" name="region" value="<%= event.dates.start.localDate %>">
                <input hidden type="text" name="subRegion" value="<%= event.dates.start.localTime %>">
                <input hidden type="text" name="mapLink" value="<%= event._embedded.venues[0].name %>">
                <input hidden type="number" name="lat" value="<%= event._embedded.venues[0].address.line1   %>">
                <label for="Interested">Interested</label>
                <input type="checkbox" name="Interested" checked>
                <label for="Attending">Attending</label>
                <input type="checkbox" name="Attending" checked>
              </fieldset>
              <input type="submit" class="btn btn-success" value="Add to interested list ">

           </form>
        </div>
      </div>
    </div>
  <% }) %>
  <% } %>
  </div>
</div>


<%- include('../partials/footer') %>
